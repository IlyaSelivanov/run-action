name: Manual Workflow
on:
  workflow_dispatch:
jobs:
  my_job:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Run Rust Action
      uses: IlyaSelivanov/github-workflow@v1.0.2
